# ARCADE-FULL — build everything

**Producer:** MacStudioGrok1  
**Mode:** Fleet command + parallel implement

## Scope

1. Ghost Hands relay (`fleet/showcase/relay.sh`)
2. Quorum commit (`fleet/showcase/quorum.py`)
3. Work-stealing swarm (`fleet/showcase/swarm.py`)
4. Leader failover script + demo (`fleet/showcase/failover_demo.sh`)
5. `fleet/fleet-status.sh` + restart includes hub-d
6. Showcase APIs + UI for all demos
7. Live tests + docs

See also `docs/SHOWCASE.md`, brainstorms in `docs/brainstorm/`.
