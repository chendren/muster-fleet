# Computer voice stack

See **[docs/VOICE.md](../../docs/VOICE.md)** for the full operator guide
(setup, API, tools, aliases, acceptance).

This directory holds the local STT / LLM / TTS implementation used by
`dashboard/server.py` and the Computer panel in `dashboard/frontend/index.html`.

**Quick start (hub):**

```bash
./download_models.sh
# then start the dashboard server from repo root
```
